import 'package:flutter/material.dart';

class Derechos extends StatelessWidget {
  const Derechos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Todos lo derecho estan reservados ©️",
      style: TextStyle(color: Colors.grey),
    );
  }
}
